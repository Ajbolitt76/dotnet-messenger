
export interface MeUserInfo {
  id: string;
  profilePhoto: string;
  userName: string;
  name: string;
  dateOfBirth: string;
}

export interface PhoneTicketInfo {
  ticket?: string;
  isLogin?: boolean;
}